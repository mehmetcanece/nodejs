const fs = require("fs");

fs.writeFileSync("hello.txt", "Hello from Node.js");
// This will create a file named hello.txt in the same directory as the script and write the text Hello from Node.js to it.
